[phases.setup]
nixPkgs = ["yt-dlp", "python3", "nodejs_22", "ffmpeg"]

[phases.build]
cmds = ["python3 -m pip install -U yt-dlp --break-system-packages"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node server.js"