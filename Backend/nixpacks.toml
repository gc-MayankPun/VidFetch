[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "nodejs_22", "ffmpeg"]

[phases.install]
cmds = ["npm install", "python3 -m pip install -U yt-dlp --break-system-packages"]

[start]
cmd = "node server.js"